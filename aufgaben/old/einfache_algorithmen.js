/**
 * Schreiben Sie eine Funktion, welche innerhalb eines
 * übergebenen Arrays einen ebenfalls übergebenen suchbegriff
 * findet, und deren Index innerhalb des Arrays zurück gibt.
 */

function findInArray(array, suchbegriff) {
    let suchbegriffIndex;


    return suchbegriffIndex;
}

const input = [ 'Ryu', 'Ken', 'Chun-Li', 'Cammy', 'Guile', 'Sakura', 'Sagat', 'Juri' ];

const result = findInArray(input, 'Cammy')

if (result == 3) 
    console.log('Richtig!')
else 
    console.log('Falsch!')

